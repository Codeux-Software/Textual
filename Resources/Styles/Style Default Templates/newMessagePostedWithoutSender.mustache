<div type="{{lineType}}" command="{{rawCommand}}" class="line {{lineClassAttributeRepresentation}}" id="line{{lineNumber}}">
	<p>
		<span class="time">{{formattedTimestamp}} </span>
		<span class="message" type="{{lineType}}">
			{{{formattedMessage}}}
		</span>
	</p>
</div>